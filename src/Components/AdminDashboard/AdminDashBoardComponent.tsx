import { Typography, Container } from "@mui/material";

const AdminDashboard = () => {
    return (
        <Container>
            <Typography variant="h4">🎛️ לוח ניהול</Typography>
            <Typography>ברוך הבא למערכת הניהול</Typography>
        </Container>
    );
};

export default AdminDashboard;
